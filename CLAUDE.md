# CLAUDE.md

Project guidance lives in @AGENTS.md — commands, the credential precedence,
the store/`useFrame` split, and the conventions the scene depends on.

`README.md` is the user-facing tour of the page and the model behind it.
